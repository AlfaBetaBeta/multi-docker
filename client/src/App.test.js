import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {});
// in a real setting we'd mock the call to the server, for now just leave dummy trivial test